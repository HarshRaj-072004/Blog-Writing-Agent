services:
  - type: web
    name: blog-writing-agent
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: streamlit run bwa_frontend.py --server.port=$PORT --server.address=0.0.0.0